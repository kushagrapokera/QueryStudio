# Flask
FLASK_SECRET_KEY=change-me-to-a-random-string

# Ollama Cloud API (for LLM-powered code/SQL generation)
OLLAMA_API_KEY=your-ollama-cloud-api-key

# MySQL (used when connecting via Direct mode in SQL tab)
QS_MYSQL_PASSWORD=your-mysql-password

# Postgres (used when connecting via Direct mode in SQL tab)
# QS_POSTGRES_PASSWORD=your-postgres-password
